const fs = require('fs');

fs.writeFileSync("filename.txt", "text to print in automatically created file in project's root directory, with name and filetype specified in previous parameter in this function.");